"use client";
import ElegantDark from "./ElegantDark";
import { EventData } from "./types";
export default function NocheDorada({ event }: { event: EventData }) {
  return <ElegantDark event={event} />;
}
